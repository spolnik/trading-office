apply plugin: 'spring-boot'

dependencies {
    compile libs.activemq,
            libs.activemq_kahadb,
            libs.spring.boot,
            libs.spring.jms
}

jar {
    baseName = 'trading-office-jms-service'
    version = '1.0.0'
}
