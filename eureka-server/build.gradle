apply plugin: 'spring-boot'

dependencies {
    compile libs.spring.boot_web,
            libs.spring.eureka_server,
            libs.springfox.swagger_ui,
            libs.springfox.swagger2
}

jar {
    baseName = 'trading-office-eureka-server'
    version = '1.0.0'
}