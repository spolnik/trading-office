apply plugin: 'spring-boot'

dependencies {
    compile libs.spring.boot,
            libs.gigaspaces,
            project(":trading-domain"),
            libs.spring.aopalliance

    testCompile libs.test.assertj
}
